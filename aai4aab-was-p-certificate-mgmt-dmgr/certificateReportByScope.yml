---
- name: First check the current certificated
  block:
    # First check the certificated
    - name: get the current certs
      include_tasks: WebsphereScripts/checkcurrentcertificates.yml
  become: true
  become_user: wasusr
  become_method: sudo

- name: ok nothing to renew... or already processed.. print the cert details
  debug:
    msg:
      - "{{ scopeType }}"
      - "{{ certificateDetails }}"
      #- "{{ certificateDetails.print }}"